[package]
name = "tavily"
version = "0.1.1"
description = "Web search as a Katari tool over the Tavily API — pure Katari, no sidecar."

[runtime]
url = "http://localhost:3000"

[dependencies]
registry = "https://raw.githubusercontent.com/katari-lang/katari-registry/main"
snapshot = "staging"
packages = []
